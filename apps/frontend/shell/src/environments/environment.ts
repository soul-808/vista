export const environment = {
  production: false,
  // Fallback URLs for remotes used during development or if remotes.json fails to load
  complianceRemote: 'http://localhost:4202/remoteEntry.js',
  infrastructureRemote: 'http://localhost:4203/remoteEntry.js',
  summaryRemote: 'http://localhost:4204/remoteEntry.js',
}; 